{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for RDAT Copilot's main window. Grants access to the Ollama-related commands and standard Tauri plugins (shell, dialog, fs, process). The updater plugin is disabled in v0.2.0 — re-enable in v0.3.0 after generating a signing keypair.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "shell:allow-open",
    "dialog:allow-open",
    "dialog:allow-save",
    "fs:allow-read-text-file",
    "fs:allow-write-text-file",
    "process:default"
  ]
}
